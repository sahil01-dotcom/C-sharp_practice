using System;

public class Program
{
    public static void Main(string[] args)
    {
        // Declare variables of different data types//

        // int - Integer data type to store whole numbers
        // double - Floating point data type to store decimal numbers
        // string - Sequence of characters to store text
        // bool - Boolean data type to store true or false values
        int age = 20;
        double height = 5.8;
        string name = "Sahil";
        char grade = 'A';   
        bool is_Student = true;

        Console.WriteLine("Name: " + name);
        Console.WriteLine("Age: " + age);
        Console.WriteLine("Height: " + height);
        Console.WriteLine("Grade: " + grade);
        Console.WriteLine("Student: " + is_Student);
    }
}